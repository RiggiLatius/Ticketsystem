services:
  app:
    build: .
    image: reisser-reklamations:latest
    container_name: reisser-reklamations
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    volumes:
      - reklamations-data:/app/backend/data

volumes:
  reklamations-data:
